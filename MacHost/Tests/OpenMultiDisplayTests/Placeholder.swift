import XCTest
@testable import OpenMultiDisplay

final class PlaceholderTests: XCTestCase {
    func testPlaceholder() {
        XCTAssertTrue(true, "Placeholder so the test target compiles before real tests land.")
    }
}
